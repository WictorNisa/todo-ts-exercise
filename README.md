# TypeScript Todo App

A simple Todo application built with React and TypeScript. This project was created to practice TypeScript with React components.


## What I Learned

- Using TypeScript with React functional components
- Type definitions for props and state
- Managing state with useState hook
- Passing functions as props
- Event handling with TypeScript
- CSS Modules for styling

## Features

- Add new todos
- Mark todos as complete/incomplete
- Delete todos
- Responsive design

## How to Run

1. Clone this repository
2. Run `npm install` to install dependencies
3. Run `npm start` to start the development server
4. Open [http://localhost:3000](http://localhost:3000) in your browser

## Project Structure

```
src/
├── components/
│   ├── TodoContainer.tsx
│   ├── TodoInput.tsx
│   ├── TodoList.tsx
│   └── TodoItem.tsx
└── types/
    └── Interfaces.ts
```

## Future Ideas

- Save todos to local storage
- Add categories for todos
- Add due dates